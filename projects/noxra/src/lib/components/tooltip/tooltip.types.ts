/**
 * Preferred side of the trigger for the tooltip.
 *
 * A preference, not a guarantee: the browser flips it when there is not enough
 * room, via `position-try-fallbacks`.
 */
export type NxTooltipPlacement = 'top' | 'bottom' | 'left' | 'right';
